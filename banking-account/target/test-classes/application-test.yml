spring:
  datasource:
    url: jdbc:h2:mem:account-testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

jwt:
  secret: "testSecretKeyForJWTThatIsLongEnough1234567890"
  expiration: 86400000
